{
  "name": "theprojectarchive",
  "version": "1.0.0",
  "description": "Static marketing site for The Project Archive",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"No tests\"",
    "build": "rm -rf dist && mkdir -p dist && cp -r *.html *.css *.js dist/ && node inject-crisp-id.js"
  },
  "dependencies": {
    "typed.js": "^2.1.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
