{
  "name": "theprojectarchive-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix next-app install",
    "dev": "npm --prefix next-app run dev",
    "build": "npm --prefix next-app run build && npm run verify-static",
    "verify-static": "node scripts/verify-static.mjs",
    "start": "npx -y serve@latest _static",
    "test": "npm --prefix next-app run test",
    "lint": "npm --prefix next-app run lint",
    "format": "npm --prefix next-app run format",
    "watch-static": "npm --prefix next-app run watch-static",
    "build:standalone": "npm --prefix next-app run build:standalone",
    "start:standalone": "npm --prefix next-app run start:standalone",
    "analyze": "npm --prefix next-app run analyze"
  },
  "type": "module"
}
