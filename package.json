{
  "name": "theprojectarchive-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["next-app"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "watch-static": "turbo run watch-static",
    "analyze": "turbo run analyze",
    "deploy": "npm run lint && npm run build && pm2 startOrReload ecosystem.config.js"
  },
  "devDependencies": {
    "pm2": "^5.3.0",
    "turbo": "^2.1.3"
  },
  "packageManager": "npm@11.4.2"
}
